<?php
/**
 * DC Service Worker Prefetcher — Server-side GA4 integration (stub).
 *
 * Reserved for server-side GA4 event sending logic extracted from the main
 * plugin file during a future modular refactor. No code yet.
 *
 * @package DC_Service_Worker_Prefetcher
 */

if ( ! defined( 'ABSPATH' ) ) {
	die();
}
